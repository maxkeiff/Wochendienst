const houseConfig = {
  roomOffset: 850,
};

const serverConfig = {
  port: 5000,
};

const dbConfig = {
  host: "",
  user: "",
  password: "",
  database: "",
};

module.exports = { serverConfig, dbConfig };
